name: Upload

on:
  push:
    branches:
      - main
  pull_request: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v6
      with:
        node-version: 24

    - name: Enable Corepack and Yarn Berry
      run: |
        corepack enable
        corepack prepare yarn@4.12.0 --activate
        yarn --version

    - uses: calcit-lang/setup-calcit@v1

    - name: Install deps
      run: caps --ci && yarn install --immutable

    - name: Validate Calcit snapshot and types
      run: |
        calcit calcit.cirru edit format
        git diff --exit-code -- calcit.cirru
        # hard gate: preprocessing must be warning-free
        calcit calcit.cirru --check-only
        # advisory: dynamic method calls cannot be monomorphized.
        # All current hits live in external modules (respo/reel/respo-alerts)
        # or documented JS FFI / alerts-plugin boundaries, so it is not a gate here.
        calcit calcit.cirru --warn-dyn-method --check-only || true
        mkdir -p .calcit/upgrade
        calcit calcit.cirru analyze check-types --summary-only --format json > .calcit/upgrade/check-types.json
        calcit calcit.cirru analyze weak-types --only schema-dynamic,code-dynamic,code-nil --intent unresolved,declared-optional --summary-only --format json > .calcit/upgrade/weak-types.json
        calcit calcit.cirru analyze deprecated --summary-only --format json > .calcit/upgrade/deprecated.json
        node scripts/check-calcit-upgrade-baseline.mjs

    - name: Compiles to JS
      run: >
        calcit calcit.cirru js && yarn vite build --base=./

    - name: Deploy to server
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      id: deploy
      uses: Pendect/action-rsyncer@v2.0.0
      env:
        DEPLOY_KEY: ${{secrets.rsync_private_key}}
      with:
        flags: '-avzr --progress'
        options: ''
        ssh_options: ''
        src: 'dist/*'
        dest: 'rsync-user@tiye.me:/web-assets/repo/${{ github.repository }}'

    - name: Display status from deploy
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: echo "${{ steps.deploy.outputs.status }}"
